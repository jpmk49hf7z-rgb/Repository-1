# Runtime
anthropic>=1.0.0
openai>=3.0.0          # also used for Perplexity's OpenAI-compatible endpoint
PyYAML>=6.0
Jinja2>=3.1

# Tests use stdlib unittest — no test dependency needed:
#   python -m unittest discover -s tests

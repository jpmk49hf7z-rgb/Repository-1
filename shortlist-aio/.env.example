# Copy to .env and fill in. Never commit .env.
#
# The scanner runs with whatever keys are present and skips the rest, so you
# can start with one engine and add the others as budget allows.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
PERPLEXITY_API_KEY=
